package io.kouna.mcgui.gui

import org.bukkit.Color
import org.bukkit.entity.TextDisplay


public open class Panel constructor() : Element() {
    override fun update(textDisplay: TextDisplay, params: AnchorParams) {
        super.update(textDisplay, params)
    }
}